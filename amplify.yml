version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm cache clean
        - npm install
        - amplify init
        - amplify env pull
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
