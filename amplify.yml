version: 1
frontend:
  phases:
    preBuild:
      commands:
        - yarn install
        - amplify init --yes
        - amplify env pull
    build:
      commands:
        - yarn run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
