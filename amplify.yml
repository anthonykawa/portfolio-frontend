version: 1
frontend:
  phases:
    preBuild:
      commands:
        - yarn cache clean
        - yarn install
        - amplify init
        - amplify env pull
    build:
      commands:
        - yarn run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
